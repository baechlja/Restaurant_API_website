ToDo:
- html:
    -add transport mode option(car, bicycle, public transport)
    -add id's for the input and buttons to use it for javascript
- java script:
    -build function to get the API
    -build function for "Reservierung"
        -save data in database (redis, MongoDB?)
    -build function for "Lieferung"
        -assign preperation time for each meal
        -use API to get the transport time
        -add preperation time and transport time and show it as total time
        -show search result
        -save data in database with sending the result

